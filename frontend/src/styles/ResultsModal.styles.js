import styled from 'styled-components';

import { ModalContent } from './Modal.styles';

export const ResultsModalContent = styled(ModalContent)`
  margin-left: 20px;
  margin-top: 30px;
  text-align: left;
`;

export const SessionDiv = styled.div`
  font-size: 14px;
  margin: 10px 0;
`;
